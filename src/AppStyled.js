import { makeStyles } from "@material-ui/core/styles";

export const AppMUIStyled = makeStyles(() => ({
  mainContainer: {
    display: "flex",
    color: "#fff",
  },
  headerSpace: {
    height: 56,
  },
}));
