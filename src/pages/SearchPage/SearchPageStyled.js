import { makeStyles } from "@material-ui/core/styles";

export const SearchPageMUIStyled = makeStyles((theme) => ({
  searchPageContainer: {
    margin: 20,
  },
  searchHeading: {
    color: "#ffc107",
    marginBottom: 8,
  },
  searchSubtitle: {
    color: "#fff",
    opacity: 0.7,
    fontSize: 16,
  },
}));
